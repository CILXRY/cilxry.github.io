# 版本文档

> Step To Step 一步一步来

> 没有 deadline 的项目是很可怕的，
> 不要坠入第一个版本还没有开发好就构思之后内容的深渊，
> 不要无限扩展，不要大步子跑。

## 0.0.0-0a

Mar 13

作为我最初应该意识到版本管理问题的一个版本，
当下做的情况其实很半吊子，
但如果这样下去就一定会落入完美主义的黑洞。

### Feat

作为一个最初版本，最不完美的版本，
但它可以挂在互联网上，挂在版本文档的第一页。

- 有一些很基础的页面：首页、归档页、友链页、博客页
- 能查看我写的文章
- 有各种各样的 Styled 卡片（比如首页上的 macOS 样式的卡片）
- 有一个超级牛逼的主题色切换功能
- 有花里胡哨的 frontmatter 系统

没了。

### 已知的 Bug

（其实 bug 有很多我只是不愿示人）

- 在特定情况下 ~~每次~~ 切换页面会导致动画重载
- 忘记给 dark 下的文字换颜色了。

问题还有不少呢。
